MapLink Server 的 Linux x86-64 与 ARM64 发行包已通过完整 Go、Web 和敏感信息检查。

每个发行包包含：

- MapLink Server 管理服务；
- 官方 `frps` 0.71.0；
- 静态管理后台；
- systemd/nftables 配置；
- 交互式安装与安全初始化脚本；
- 部署和 API 文档。

请下载对应架构的 `.tar.gz`，并先使用同名 `SHA256SUMS.txt` 校验完整性。
