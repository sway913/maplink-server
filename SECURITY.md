# 安全策略

## 报告漏洞

请不要在公开 Issue 中提交 Token、密码、私钥、证书私钥、真实服务器地址或可直接利用的漏洞细节。可通过 GitHub Security Advisory 的私密报告功能联系维护者。

报告中请包含受影响版本、复现条件、预期影响和建议修复方式，但务必使用测试凭据与保留地址。

## 部署责任

- 使用可信 HTTPS 证书并保持系统、FRP 与 MapLink Server 更新。
- 不向公网开放 FRP 原生 Dashboard 端口。
- 限制管理端口来源地址，并为管理账户使用唯一强密码。
- 定期轮换客户端 Token；轮换后及时更新所有客户端。
- 将 `/etc/frp-manager` 和 `/etc/frp` 视为敏感配置目录。
- 只开放实际需要的映射端口段。

本项目不会要求上传 SSH 私钥。任何要求复制、提交或发送私钥的说明都不是正常 MapLink 工作流程。
